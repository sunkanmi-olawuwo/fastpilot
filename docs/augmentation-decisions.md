# Augmentation Decisions (Week 6) — BONUS

> **Status:** scaffold (Phase 0). Written in Phase 5. Components live in `app/augmentations/`.

## Gap identified
_TODO: retrieval answers *how*, but CODE_GENERATION answers were unverified — users still
had to write and test the code themselves._

## Augmentation selected
_TODO: grounded code-runner with CRAG-style self-correction + security guards; Playground (D11)
as the user-facing extension (same sandbox)._

## Measurement
_TODO (Phase 4): success rate with self-correction ON vs OFF, mean attempts, mean latency, grounding spot-check._

## Honest limits
_TODO: single-file apps only, no DB, no network; no keystroke autocomplete (Streamlit/iframe constraint)._
